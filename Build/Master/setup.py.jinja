from setuptools import find_packages, setup


setup(
    name='{{name}}',
    packages=find_packages(include=['datalabs']),
    version='{{version}}',
    description='{{description}}',
    author='DataLabs@ama-assn.org',
    license='None',
    install_requires=[{{requirements}}],
)
